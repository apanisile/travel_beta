import 'package:flutter/material.dart';
import 'widgets/scaffold.dart';

class GreecePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return buildEasyScaffold(
      appBarTitle: 'Greece',
      body: Container(),
    );
  }
}
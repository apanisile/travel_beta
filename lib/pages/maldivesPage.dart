import 'package:flutter/material.dart';
import 'widgets/scaffold.dart';

class MaldivesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return buildEasyScaffold(
      appBarTitle: 'Maldives',
      body: Container(),
    );
  }
}
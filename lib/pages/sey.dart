import 'package:flutter/material.dart';
import 'widgets/scaffold.dart';

class SeyPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return buildEasyScaffold(
      appBarTitle: 'Saychelles',
      body: Container(),
    );
  }
}